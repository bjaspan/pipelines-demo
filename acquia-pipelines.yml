version: 1.0.0

events:
  build:
    steps:
      - main:
          script:
            - set -x
            - printenv
            - ls $HOME/.ssh
            - cat $HOME/.ssh/authorized_keys
            - hostname
            - cat /etc/hostname
