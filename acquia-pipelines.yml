version: 1.0.0

events:
  build:
    steps:
      - build:
          type: script
          script:
            - mkdir -p ./docroot
            - echo 'Hello, Pipelines.txt' > ./docroot/hello.txt
            
