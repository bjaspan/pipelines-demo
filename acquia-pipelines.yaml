version: 1.0.0

events:
  build:
    steps:
      - node:
          script:
            - nvm install node
            - nvm use node
            - npm i -g angular-cli
      - build:
          script:
            - ng build -prod --output-path=/tmp/build
      - copy_dist:
          script:
            - rm -rf *
            - cp -r /tmp/build/ .
